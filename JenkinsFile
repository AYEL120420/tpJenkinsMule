pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                script {
                    // Construire avec Maven
                    sh 'mvn clean package'
                }
            }
        }
}
